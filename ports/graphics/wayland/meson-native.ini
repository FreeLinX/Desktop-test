# FreeLinX/ports - graphics/wayland : meson native file pointing at the
# host-built wayland-scanner (used to generate protocol code during the
# cross build).
[binaries]
wayland-scanner = '@DIRNAME@/../../../ports/build/host-wl/bin/wayland-scanner'

[properties]
