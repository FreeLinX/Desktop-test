# Build trees and large artifacts (never commit)
src/build/
kernel/build/
ports/build/
ports/work/
ports/obj/
ports/staging/
ports/dist
ports/dist/
toolchain
toolchain/
qemu/
.venv/
__pycache__/
st-src/st
st-src/*.o

# Merged runtime trees inside staging rootfs that mirror ports deps
ports/build/deps/

# Images / binaries (keep bzImage but drop the big initramfs copies)
src/build/*.img.gz
iso/*.img.gz
iso/*.img
*.iso

# Test logs and captures
*.log
*.ppm
*.raw
*.png
*.pid
vmtest/serial.log
vmtest/qemu.log
vmtest/scratch*.log
vmtest/*.pid

# Editor / OS noise
*.swp
*~
.DS_Store

# Nested working repos
drivers/.git/
# local QEMU test disks (keep untracked - never commit)
disk.img
flx-install.img
flx-fresh.img
flx-win.img
iso/*.img
iso/*.img.gz.bak*
