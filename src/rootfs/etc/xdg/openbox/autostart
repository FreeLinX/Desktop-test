#
# FreeLinX Openbox session autostart.
# These things are run when the Openbox X Session starts.
# The user copy is $HOME/.config/openbox/autostart.
#

# Background colour (best effort). Plain black desktop.
if command -v xsetroot >/dev/null 2>&1; then
    xsetroot -solid "#000000" 2>/dev/null
fi