#!/bin/sh
# FreeLinX Browser Launcher (non-GNU)
# Launches Links in graphical mode (-g) if X11 is running, or text mode in console.

HOMEPAGE="/usr/share/freelinx/welcome.html"
URL="${1:-$HOMEPAGE}"

if [ -n "${DISPLAY:-}" ]; then
    exec /usr/bin/links -g "$URL"
else
    exec /usr/bin/links "$URL"
fi
